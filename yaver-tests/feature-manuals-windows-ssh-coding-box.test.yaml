# feature-manuals-windows-ssh-coding-box — manual: MacBook to Windows AI box
# over SSH.
name: feature-manuals-windows-ssh-coding-box
target: web
url: http://127.0.0.1:3000
viewport: { width: 1366, height: 900 }
timeout_ms: 15000

artifacts:
  on: always
  screenshot: true
  video: true

capture:
  console_errors: true
  network: true

steps:
  - goto: /manuals/windows-ssh-coding-box
  - wait_for: h1
  - assert.text: MacBook to Windows AI box over SSH
  - screenshot: true
