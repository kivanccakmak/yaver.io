# feature-manuals-relay-setup — manual: Yaver Relay Setup.
name: feature-manuals-relay-setup
target: web
url: http://127.0.0.1:3000
viewport: { width: 1366, height: 900 }
timeout_ms: 15000

artifacts:
  on: always
  screenshot: true
  video: true

capture:
  console_errors: true
  network: true

steps:
  - goto: /manuals/relay-setup
  - wait_for: h1
  - assert.text: Yaver Relay Setup
  - assert.text: remote path for reaching your machine
  - screenshot: true
