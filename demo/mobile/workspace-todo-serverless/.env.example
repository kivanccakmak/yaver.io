# Public routing only. Never place a pp_ project token in an EXPO_PUBLIC_ variable:
# the app accepts it at runtime and stores it in the device keychain.
EXPO_PUBLIC_YAVER_SERVERLESS_URL=
EXPO_PUBLIC_YAVER_PROJECT_SLUG=workspace-todo-serverless
