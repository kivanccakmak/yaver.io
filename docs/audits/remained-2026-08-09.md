# REMAINED — Next-Session Dump & Deep Audit (2026-08-09, late)

## 1. Repo state

- Branch `main`; remote **`github`** (`git@github.com:yaver-io/yaver.io.git`). Push with `git push github main`.
- HEAD: `33b3d798e` — `fix(mobile): remove Chat|Terminal toggle — tasks are chat-only` (pushed).
- Prior commit this session: `d15ba042c` — top-level-only projects + agent→Convex per-machine catalog seeding (pushed).
- A **parallel session is active** in this checkout and committing (it swept the mobile/devices work, deleted my discovery tests once, and is mid-work on `mcp_vision.go` / `vision_cmd.go` / `VisionSettingsCard` / `desktop/agent/{config,set_cmd,main,mcp_*}.go`). **Stage only your own files; never `git add -A`.**

## 2. Working tree right now

Mine (mine only, need verify+commit):
- `web/app/dashboard/page.tsx` — Chat|Terminal removal applied; **one leftover unused `const isOpenCodeTask` at L999** must be deleted, then web tsc.

Parallel session's (do NOT touch): `desktop/agent/{config,httpserver,main,mcp_instructions,mcp_selenium,mcp_tools,set_cmd}.go`, `mobile/app/(tabs)/settings.tsx`, `mobile/src/lib/quic.ts`, `web/components/dashboard/SettingsView.tsx`, `web/lib/agent-client.ts`, new `mcp_vision.go`, `vision_cmd.go`, `VisionSettingsSection.tsx`, `VisionSettingsCard.tsx`, `opencode-plugins/`, `docs/audits/opencode-vision-mcp-audit-2026-08.md`.

Noise (gitignored/intent): `yaver-tests/.coverage.json`, temp `e2e/_open-for-user.mjs`.

## 3. What is REMAINING (already coded, needs verify + commit)

| # | Item | File(s) | Action |
|---|------|---------|--------|
| R1 | Web Chat|Terminal removal — finish | `web/app/dashboard/page.tsx` | delete unused `isOpenCodeTask` (L999); `npx tsc --noEmit`; commit + push (mirror of `33b3d798e`) |

## 4. What is NOT STARTED (open items, user-priority order)

| # | Item | Evidence / expected behaviour |
|---|------|------------------------------|
| N1 | **Follow Up composer spinner** | `mobile/app/(tabs)/tasks.tsx:7388` — `{isRunning && <ActivityIndicator/>}` in the expanded Follow Up header next to `opencode · DeepSeek V4 Flash ▾` + `medici.ai · No MCPs ▾`. Running state is ALREADY named 3× on screen (status pill, PhaseStatusLine, ■ Stop). Remove the line (LESS-IS-MORE: a widget that restates what's shown is noise; a pulsing circle beside a usable composer reads as blocked). |
| N2 | **CommandCard "(no output captured)"** | User's medici task: `$ ls -la /root/Workspace/medici.ai` → `done` → `(no output captured)` on unfold. `mobile/src/components/CommandCard.tsx:104` shows it when `model.stdout/stderr` empty. Cards come from `command_start/output/end` SSE events (`mobile/src/lib/commandEvents.ts`, mirror of `desktop/agent/command_events.go`). So `start`+`end` arrived (status "done") but **no `command_output`** — root cause to find in `desktop/agent/command_events.go` (likely: opencode runner stdout rides the `raw` lane, never the structured `command_output` lane). Expected: captured stdout always renders on unfold; never "(no output captured)" for a command that ran. |
| N3 | **"React Native WebView does not support this platform."** | 14 files still import raw `react-native-webview` (incl. `gateway-gates.tsx`, `dashboard-view.tsx`, `tutorials.tsx`, `remote-runtime.tsx`, `attach.tsx`, `glass-workspace.tsx`, `droid-control.tsx`, `XtermView.tsx`, `StlViewer.tsx`, `CameraStream.tsx`, `SpatialPreview.tsx`, `HiddenPackageWebView.tsx`). `WebViewCompat.tsx`+`.web.tsx` already exists as the additive browser-safe seam (apps.tsx/project.tsx use it). Expected: the RN-web browser lane never throws; unsupported surfaces route through `WebViewCompat` or show `explainNoTransport()`-style honesty. Native files unchanged. |
| N4 | **Claude Code mobile-style tasks UI** | Style the mobile task/composer surface like Claude Code mobile's "remote tasks" (runner/model chip, follow-up affordance), uniform for whatever runner is remote — opencode/glm/deepseek/claude/codex. Current runner chip shows `opencode · DeepSeek V4 Flash`; make the style runner-agnostic and Claude-Code-mobile-like. |
| N5 | **e2e closed loop — medici / ai.tusrehber.com (text→text)** | New spec (model on `project-chip-mobile.spec.ts`): RN-web in iPhone 15 Pro context → pick `medici.ai` project → send a text prompt → assert the text reply streams into the chat. Also fold in N2's assertion (a command's stdout must render on unfold). |

## 5. What was EXPECTED (the session's requirement list)

1. Open the mobile app from Chrome automation with the real device viewport. ✅ done (`project-chip-mobile.spec.ts` + headful Chrome).
2. No chat/console discrimination — **mobile + web must not have the Chat|Terminal toggle at all**. ✅ mobile done (`33b3d798e`); ⏳ web half-done (R1).
3. Project selection by tapping the chip. ✅ done (in-composer overlay picker; e2e passed).
4. Projects = top-level git projects (medici.ai / yaver.io / talos / sfmg), combining **Convex seeded data + Go agent discovery**, never "talos backend / talos frontend / yaver mobile". ✅ done (`d15ba042c`).
5. Yaver seeding of projects/git-provider/machine into Convex via the Go agent, with web + mobile UI plumbing. ✅ done (`d15ba042c` + `projectTopLevel.ts` merges).
6. Captured task output must show on unfold. ❌ **N2 open.**
7. No console thing in both surfaces; no WebView platform error. ⏳ R1 + ❌ N3 open.
8. Snowball-focused tests throughout. ✅ Go tests + web tests + `project-chip-mobile.spec.ts`; ⏳ N5 (medici text→text) + N2 assertion open.

## 6. Execution plan (Build mode order)

1. **Write this dump** to `docs/audits/remained-2026-08-09.md`.
2. **R1** — delete `isOpenCodeTask` L999 in `web/app/dashboard/page.tsx`; `cd web && npx tsc --noEmit`; commit `fix(web): remove Chat|Terminal toggle — dashboard is chat-only`; push.
3. **N1** — delete the `{isRunning && <ActivityIndicator/>}` line (L7388) from the follow-up composer header; mobile tsc; commit `fix(mobile): drop redundant running spinner from Follow Up composer`; push.
4. **N2** — audit `desktop/agent/command_events.go` + `mobile/src/lib/commandEvents.ts`: why opencode shell commands emit start/end but no output. Fix (emit `command_output` for the raw lane, or fall back to the transcript span on `command_end`). Go test + e2e assertion. Commit.
5. **N3** — route the 14 raw `react-native-webview` consumers through `WebViewCompat` (or add `.web.tsx` no-ops / honest `explainNoTransport()`); native unchanged; verify by running RN-web on each surface. Commit.
6. **N4** — Claude-Code-mobile styling pass on the task/composer (runner-agnostic). Commit.
7. **N5** — new e2e spec `medici-text-loop.spec.ts` (medici.ai project, text→text, output-on-unfold assertion). Run the full mobile lane (`project-chip-mobile.spec.ts` + new spec). Commit.
8. **Final** — mobile + web tsc clean; e2e green; push all.

## 7. Risks / constraints

- **Parallel session** is editing the same checkout; stage only your files; re-check `git status` before every commit. It already deleted my discovery tests once and swept DevicesView — watch for it.
- **Deploy is owner-only** — none of this deploys without your explicit go-ahead (`./deploy/deploy.sh backend/web/npm/ios`).
- **Go build/test** earlier hit a hang in the full suite; use focused runs (`-run 'TestCollapseNestedRepos|TestDeriveGitProvider|TestBuildRuntimeProjectCatalog|TestBatchPayloadIncludes|TestGitWalk'`).
- The headful Chrome for manual testing is open at the login screen (RN-web :8081); sign in to test.
